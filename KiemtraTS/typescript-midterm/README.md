# Bài kiểm tra giữa khóa TypeScript

Chương trình quản lý Customer, Employee và Project bằng dữ liệu lưu trong Array.

## Cài đặt và chạy

```bash
npm install
npm test
```

Hoặc chạy riêng từng bước:

```bash
npm run build
npm start
```

Mã nguồn chính nằm tại `src/index.ts`.
